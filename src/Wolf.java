public class Wolf extends Predator{
    public Wolf(String name,int eat,int move,int multiply){
        super("Волк", 10, 10, 20);
        if( (int) eatProbably > 20 && 80 > eatProbably ){
            Object Horce = null;
            eat += 10;
        } else if (eatProbably > 80 && 100 > eatProbably) {
            //Типо должно съесть другое животное
        }
    }
}
